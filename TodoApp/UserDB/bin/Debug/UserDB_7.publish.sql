/*
Deployment script for MinimalAPIUserDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "MinimalAPIUserDB"
:setvar DefaultFilePrefix "MinimalAPIUserDB"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating Procedure [dbo].[sp_Company_Delete]...';


GO
CREATE PROCEDURE [dbo].[sp_Company_Delete]
	@Id int
AS
BEGIN
	DELETE 
	FROM dbo.[Company]
	WHERE Id = @Id;
END
GO
PRINT N'Creating Procedure [dbo].[sp_Company_Get]...';


GO
CREATE PROCEDURE [dbo].[sp_Company_Get]
	@Id int
AS
BEGIN
	SELECT Id,FirstName, LastName, Country
	FROM dbo.[Company]
	WHERE Id = @Id;
END
GO
PRINT N'Creating Procedure [dbo].[sp_Company_GetAll]...';


GO
CREATE PROCEDURE [dbo].[sp_Company_GetAll]
AS
BEGIN
SELECT * FROM [dbo].[Company]
END
GO
PRINT N'Creating Procedure [dbo].[sp_Company_Insert]...';


GO
CREATE PROCEDURE [dbo].[sp_Company_Insert]
	@FirstName nvarchar(50),
	@LastName nvarchar(50),
	@Country nvarchar(50)
AS

BEGIN
	INSERT INTO dbo.[Company] (FirstName, LastName,Country)
	VALUES (@FirstName,@LastName,@Country);
END
GO
PRINT N'Creating Procedure [dbo].[sp_Company_Update]...';


GO
CREATE PROCEDURE [dbo].[sp_Company_Update]
	@Id int,
	@FirstName nvarchar(50),
	@LastName nvarchar(50),
	@Country nvarchar(50)
AS
BEGIN
	UPDATE dbo.[Company]
	SET FirstName = @FirstName, LastName = @LastName, Country = @Country
	WHERE Id = @Id;
END
GO
if not exists (select 1 from dbo.[User])
begin
	insert into dbo.[User] (FirstName, LastName)
	values ('Cuca', 'Beludo'),
	('Valentin','Terra'),
	('Paula','Tejando'),
	('Helma','Marei');
end
GO

GO
PRINT N'Update complete.';


GO
